# Leave VITE_API_URL empty so config.ts auto-detects the backend:
#   - nlex.tech → https://api.nlex.tech
#   - any other host → http://localhost:8000
# Override by setting VITE_API_URL in your .env.local or as a Docker build arg.
VITE_API_URL=
